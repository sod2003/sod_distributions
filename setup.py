from setuptools import setup

setup(name='sod_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['sod_distributions'],
      author = "Sean O'Dowd",
      author_email = 'sod2003@gmail.com',
      zip_safe=False)
